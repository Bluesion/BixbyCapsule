action (BodyPartSelection) {
  description (스트레칭할 신체부위를 고르는 Action)
  collect {
    input (stretching) {
      type (Stretching)
      min (Optional) max (One)
    }
    input (speech) {
      type (Speech)
      min (Optional) max (One)
    }
  }
  type (Search)
  output (BodyPartOutput)
}
